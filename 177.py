# Initialize a list of numbers with some duplicates
numbers = [1, 2, 2, 3, 4, 4, 5, 5]

# Create a tuple of unique elements using a set and then converting it to a tuple
unique_tuple = tuple(set(numbers))

# Print the original list
print(numbers)

# Print the tuple containing unique elements
print(unique_tuple)
