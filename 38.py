# Sample floating-point numbers
values = [-45.56, 0.1]

# Calculate and display the absolute values
for value in values:
    absolute_value = abs(value)
    print(f"Values = {value}")
    print(f"Absolute Values = {absolute_value}\n")
